# Euclidean remainder

For an integer `a` and positive modulus `m`, Euclidean division gives a unique
remainder `r` such that `a = qm + r` and `0 <= r < m`. This example implements
that definition for the fixed modulus 97. The local page reference in the
manifest is a stable locator for this compact teaching source.
