version: '3'

vars:
  APP_NAME: myproject
  MAIN_PKG: ./cmd/{{.APP_NAME}}
  GOLANGCI_LINT_VERSION: v2.12.2
  GITLEAKS_VERSION: v8.18.4
  GOFUMPT_VERSION: v0.10.0
  GOIMPORTS_VERSION: v0.48.0
  DEFAULT_GOCACHE: '{{.TMPDIR | default "/tmp"}}/go-agent-first-arch/go-build'
  DEFAULT_CCACHE_DIR: '{{.TMPDIR | default "/tmp"}}/go-agent-first-arch/ccache'
  DEFAULT_GOLANGCI_LINT_CACHE: '{{.TMPDIR | default "/tmp"}}/go-agent-first-arch/golangci-lint'

silent: false

# Keep Go, ccache, and golangci-lint state out of a possibly read-only home
# directory. Callers can still supply their own cache locations explicitly.
env:
  GOCACHE: '{{.GOCACHE | default .DEFAULT_GOCACHE}}'
  CCACHE_DIR: '{{.CCACHE_DIR | default .DEFAULT_CCACHE_DIR}}'
  GOLANGCI_LINT_CACHE: '{{.GOLANGCI_LINT_CACHE | default .DEFAULT_GOLANGCI_LINT_CACHE}}'

tasks:
  default:
    desc: List available tasks
    cmds:
      - task --list

  orient:
    desc: Orient an agent entering the repository (cells + doctor + status)
    cmds:
      - task: index
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - task: cells
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - task: doctor
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - git --no-pager status --short
      - 'echo "→ Use: task find-cell QUERY=<topic> to find a cell"'
      - 'echo "→ Use: task context ID=<cell-id> to read a cell"'

  changed:
    desc: Summarize current work and likely validation needs (status + impact)
    cmds:
      - git --no-pager status --short
      - task: impact
        vars: {ROOT: '{{.ROOT | default "."}}'}

  ready:
    desc: Standard pre-handoff validation for agents (doctor + impact + test)
    cmds:
      - task: doctor
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - task: impact
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - |
        root="{{.ROOT | default "."}}"
        if [ "$root" = "." ]; then
          task test
        else
          (cd "$root" && go test -race -count=1 ./...)
        fi
      - git --no-pager status --short
      - 'echo "✓ Ready for handoff"'

  quick-check:
    desc: Fast post-edit validation (structure + policy + manifests + index)
    cmds:
      - task: structure-test
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - task: policy
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - task: check-manifests
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - task: check-index
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - 'echo "✓ Quick check passed"'

  setup:
    desc: Bootstrap the local developer/agent environment
    cmds:
      - task: tools:check
      - task: install-hooks
      - 'echo "✓ Setup complete — run: task orient"'

  tools:check:
    desc: Verify required development tools are installed
    cmds:
      - |
        tools="go task gofumpt goimports golangci-lint gitleaks govulncheck"
        missing=0
        for tool in $tools; do
          if command -v "$tool" >/dev/null 2>&1; then
            case "$tool" in
              go) version="$(go version)" ;;
              goimports) version="$(go version -m "$(command -v goimports)" 2>/dev/null | awk '$1 == "mod" { print $3; exit }')"; version="${version:-unknown}" ;;
              *) version="$("$tool" --version 2>&1 | head -1)" ;;
            esac
            echo "  ✓ $tool ($version)"
          else
            echo "  ✗ $tool NOT FOUND"
            missing=$((missing + 1))
          fi
        done
        if [ "$missing" -gt 0 ]; then
          echo ""
          echo "$missing tool(s) missing. Run: task tools:install"
          exit 1
        fi
        echo ""
        echo "✓ All tools present"

  tools:install:
    desc: Install required Go development tools
    cmds:
      - go install mvdan.cc/gofumpt@{{.GOFUMPT_VERSION}}
      - go install golang.org/x/tools/cmd/goimports@{{.GOIMPORTS_VERSION}}
      - go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@{{.GOLANGCI_LINT_VERSION}}
      - go install github.com/gitleaks/gitleaks/v8@{{.GITLEAKS_VERSION}}
      - go install golang.org/x/vuln/cmd/govulncheck@latest
      - 'echo "✓ Tools installed"'

  all:
    desc: Build, test, lint, check files, manifests, indexes, and AGENTS.md
    cmds:
      - task: build
      - task: test
      - task: lint
      - task: check-files
      - task: check-manifests
      - task: check-index
      - task: check-agents
      - echo "✓ All checks passed"

  build:
    desc: Compile the binary
    cmds:
      - go build -o bin/{{.APP_NAME}} {{.MAIN_PKG}}

  test:
    desc: Run all tests with race detector
    cmds:
      - task: test-unit
      - task: test-integration
      - task: test-reference
      - task: test-library
      - task: test-research-library
      - task: test-module-root-library

  test-unit:
    desc: Run bootstrap unit and tooling tests (fast, no external deps)
    cmds:
      - |
        for dir in internal pkg tools/agent; do
          if [ -d "$dir" ]; then
            go test -race -count=1 -short ./$dir/...
          fi
        done

  test-integration:
    desc: Run integration tests (may need external services)
    cmds:
      - |
        if [ -d test/integration ]; then
          go test -race -count=1 ./test/integration/...
        fi

  test-reference:
    desc: Run the reference project's tests with the race detector
    cmds:
      - (cd examples/reference-project && go test -race -count=1 ./...)

  test-library:
    desc: Run the library example's tests with the race detector
    cmds:
      - (cd examples/library-project && go test -race -count=1 ./...)

  test-research-library:
    desc: Run the research-library example's tests with the race detector
    cmds:
      - (cd examples/research-library-project && go test -race -count=1 ./...)

  test-module-root-library:
    desc: Run the module-root library example's tests with the race detector
    cmds:
      - (cd examples/module-root-library-project && go test -race -count=1 ./...)

  fuzz:
    desc: "Bounded manifest-parser fuzz test. Optional: FUZZ_TIME=30s"
    cmds:
      - go test -fuzz=FuzzParse -fuzztime={{.FUZZ_TIME | default "10s"}} -parallel=1 ./tools/agent/manifest

  test-cell:
    desc: "Run tests for a specific cell. Usage: task test-cell ID=user-authenticate"
    requires:
      vars: [ID]
    cmds:
      - |
        root="{{.ROOT | default "."}}"
        path=$(go run ./tools/agent/cell-path/ --root "$root" {{.ID}})
        (cd "$root" && go test -race -count=1 -v "./$path/...")

  lint:
    desc: Run golangci-lint for the bootstrap and reference modules
    cmds:
      - golangci-lint run ./...
      - (cd examples/reference-project && golangci-lint run ./...)
      - (cd examples/library-project && golangci-lint run ./...)

  lint-cell:
    desc: "Lint a specific cell. Usage: task lint-cell ID=user-authenticate"
    requires:
      vars: [ID]
    cmds:
      - |
        root="{{.ROOT | default "."}}"
        path=$(go run ./tools/agent/cell-path/ --root "$root" {{.ID}})
        (cd "$root" && golangci-lint run "./$path/...")

  check-files:
    desc: Enforce max file size (300 LOC)
    cmds:
      - |
        find . -name '*.go' -not -path './vendor/*' -not -path './tools/agent/*' -not -name '*_test.go' |
        xargs wc -l |
        awk '$1 > 300 && $2 != "total" {print "FILE TOO LARGE:", $2, "("$1" lines)"; exit 1}'
      - echo "✓ All files within size limits"

  check-manifests:
    desc: Validate all cell.yaml manifests
    cmds:
      - go run ./tools/agent/index/ --json --root "{{.ROOT | default "."}}" >/dev/null
      - echo "✓ All manifests valid"

  index:
    desc: "Regenerate indexes. Optional: ROOT=<project-root>"
    cmds:
      - go run ./tools/agent/index/ --root "{{.ROOT | default "."}}"
      - echo "✓ Indexes regenerated"

  check-index:
    desc: "Verify indexes are fresh. Optional: ROOT=<project-root>"
    cmds:
      - go run ./tools/agent/index/ --check --root "{{.ROOT | default "."}}"
      - echo "✓ Indexes are fresh"

  index-json:
    desc: "Output index JSON. Optional: ROOT=<project-root>"
    cmds:
      - go run ./tools/agent/index/ --json --root "{{.ROOT | default "."}}"

  impact:
    desc: "Show affected cells for changed files (blast-radius map). Optional: ROOT=<project-root>"
    cmds:
      - go run ./tools/agent/impact/ --root "{{.ROOT | default "."}}"

  impact-json:
    desc: "Show affected cells for changed files as JSON. Optional: ROOT=<project-root>"
    cmds:
      - go run ./tools/agent/impact/ --json --root "{{.ROOT | default "."}}"

  scope:
    desc: "Print the explicit pre-edit boundary for a cell. Usage: task scope ID=user-authenticate"
    requires:
      vars: [ID]
    env:
      SCOPE_ROOT: '{{.ROOT | default "."}}'
      SCOPE_ID: '{{.ID}}'
    cmds:
      - go run ./tools/agent/scope/ --root "$SCOPE_ROOT" "$SCOPE_ID"

  verify-scope:
    desc: "Fail if changes exceed a declared cell scope. Optional: WITH=users,@wiring"
    requires:
      vars: [ID]
    env:
      SCOPE_ROOT: '{{.ROOT | default "."}}'
      SCOPE_WITH: '{{.WITH | default ""}}'
      SCOPE_ID: '{{.ID}}'
    cmds:
      - go run ./tools/agent/scope/ --verify --root "$SCOPE_ROOT" --with "$SCOPE_WITH" "$SCOPE_ID"

  new-cell:
    desc: "Scaffold a lean private cell. Optional: ROOT=<project-root>"
    requires:
      vars: [ID]
    cmds:
      - ./tools/agent/new-cell.sh --root "{{.ROOT | default "."}}" {{.ID}}

  new-cell-ext:
    desc: "Scaffold an extended application cell (service, store, handler). Optional: ROOT=<project-root>"
    requires:
      vars: [ID]
    cmds:
      - ./tools/agent/new-cell.sh --extended --root "{{.ROOT | default "."}}" {{.ID}}

  conformance:
    desc: "Validate and display a public package's research conformance. Usage: task conformance ID=field"
    requires:
      vars: [ID]
    cmds:
      - go run ./tools/agent/conformance/ --root "{{.ROOT | default "."}}" {{.ID}}

  new-package:
    desc: "Scaffold a public library package. Usage: task new-package ID=field [PATH=field]"
    requires:
      vars: [ID]
    cmds:
      - go run ./tools/agent/new-package/ --root "{{.ROOT | default "."}}" --id {{.ID}} --path "{{.PATH | default ""}}" --package "{{.PACKAGE | default ""}}"

  scaffold-domain:
    desc: "Scaffold a private domain cell. Optional: ROOT=<project-root>"
    requires:
      vars: [ID]
    cmds:
      - ./tools/agent/new-domain.sh --root "{{.ROOT | default "."}}" {{.ID}}

  validate-cell:
    desc: "Run cell's declared validation commands. Usage: task validate-cell ID=user-authenticate"
    requires:
      vars: [ID]
    cmds:
      - go run ./tools/agent/validate-cell/ --root "{{.ROOT | default "."}}" {{.ID}}

  policy:
    desc: Validate import policy
    cmds:
      - go run ./tools/agent/imports/ --root "{{.ROOT | default "."}}"

  vuln:
    desc: Run govulncheck
    cmds:
      - govulncheck ./...

  secrets:
    desc: Scan the working tree for hardcoded secrets
    cmds:
      - gitleaks dir --redact .

  secrets-history:
    desc: Scan all reachable Git history for hardcoded secrets
    cmds:
      - gitleaks git --redact --log-opts="--all"

  generate:
    desc: Run all go:generate directives
    cmds:
      - go generate ./...

  fmt:
    desc: Format all Go files
    cmds:
      - gofumpt -w .
      - goimports -w .

  clean:
    desc: Remove build artifacts
    cmds:
      - rm -rf bin/

  structure-test:
    desc: Run architecture invariant tests
    cmds:
      - STRUCTURE_ROOT="{{.ROOT | default "."}}" go test -run "Test(Cell|No|Files|Every|Configured)" ./tools/agent/structure/...

  cells:
    desc: "List cells. Optional: ROOT=<project-root>"
    cmds:
      - go run ./tools/agent/cells/ --root "{{.ROOT | default "."}}"

  cells-json:
    desc: "List cells as JSON. Optional: ROOT=<project-root>"
    cmds:
      - go run ./tools/agent/cells/ --json --root "{{.ROOT | default "."}}"

  deps:
    desc: "Show dependencies. Optional: ROOT=<project-root>"
    requires:
      vars: [ID]
    cmds:
      - go run ./tools/agent/deps/ --root "{{.ROOT | default "."}}" {{.ID}}

  deps-json:
    desc: "Show dependencies as JSON. Optional: ROOT=<project-root>"
    requires:
      vars: [ID]
    cmds:
      - go run ./tools/agent/deps/ --root "{{.ROOT | default "."}}" {{.ID}} --json

  context:
    desc: "Print a cell's context pack. Usage: task context ID=user-authenticate"
    requires:
      vars: [ID]
    cmds:
      - go run ./tools/agent/index/ --check --root "{{.ROOT | default "."}}" 2>/dev/null || go run ./tools/agent/index/ --root "{{.ROOT | default "."}}"
      - cat "{{.ROOT | default "."}}/gen/context/{{.ID}}.context.md"

  context-json:
    desc: "Print a cell's context pack as JSON. Usage: task context-json ID=user-authenticate"
    requires:
      vars: [ID]
    cmds:
      - go run ./tools/agent/index/ --check --root "{{.ROOT | default "."}}" 2>/dev/null || go run ./tools/agent/index/ --root "{{.ROOT | default "."}}"
      - python3 -c 'import json, pathlib; print(json.dumps({"id":"{{.ID}}","context":pathlib.Path("{{.ROOT | default "."}}/gen/context/{{.ID}}.context.md").read_text()}, indent=2))'

  find-cell:
    desc: "Search cells. Optional: ROOT=<project-root>"
    requires:
      vars: [QUERY]
    cmds:
      - go run ./tools/agent/find-cell/ --root "{{.ROOT | default "."}}" "{{.QUERY}}"

  doctor:
    desc: Run all architecture checks (fast, no build or tests)
    cmds:
      - task: check-manifests
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - task: check-index
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - task: structure-test
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - task: check-agents
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - task: policy
        vars: {ROOT: '{{.ROOT | default "."}}'}
      - 'echo "✓ Doctor: all architecture checks passed"'

  check-agents:
    desc: Validate AGENTS.md files have required sections
    cmds:
      - go run ./tools/agent/check-agents/ --root "{{.ROOT | default "."}}"

  install-hooks:
    desc: Install git hooks from .githooks/ directory
    cmds:
      - git config core.hooksPath .githooks
      - chmod +x .githooks/pre-commit .githooks/pre-push
      - echo "✓ Git hooks installed (core.hooksPath = .githooks)"

  help:
    desc: Show available tasks
    cmds:
      - task --list
